import { RoomSelectedPipe } from './room-selected.pipe';

describe('RoomSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new RoomSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
